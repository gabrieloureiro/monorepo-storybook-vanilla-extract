import { Meta } from '@storybook/addon-docs'
import { TokensTable } from '../../components/TokensTable'
import { tokens } from '@loureiro/web'


<Meta title="Tokens/Font Weights" />

# Font Weights

This is a list of all the font weights used in the design system.

<TokensTable tokens={tokens.fontWeights} />
